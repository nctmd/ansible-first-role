---

- name: install packs
  hosts: all
  vars:
    install_state: installed

  tasks:
    - name: install mariadb
      yum:
        name:
          - telnet
          - mlocate
          - bind-utils
        state: "{{ install_state }}"

